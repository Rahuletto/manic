{
  "name": "manic-monorepo",
  "devDependencies": {
    "@types/bun": "latest",
    "oxc-minify": "^0.125.0",
    "oxlint": "^1.60.0",
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "dev": "cd demo && bun run dev",
    "build": "cd demo && bun run build",
    "typecheck": "cd packages/manic && bun run tsc --noEmit"
  },
  "workspaces": [
    "packages/*",
    "plugins/*",
    "demo",
    "docs",
    "examples/*"
  ]
}
