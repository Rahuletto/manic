{
  "name": "manic-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "plugins/*",
    "demo",
    "examples/*"
  ],
  "scripts": {
    "dev": "cd demo && bun run dev",
    "build": "cd demo && bun run build",
    "typecheck": "cd packages/manic && bun run tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxc-minify": "^0.125.0",
    "oxlint": "^1.60.0",
    "typescript": "^5"
  }
}
